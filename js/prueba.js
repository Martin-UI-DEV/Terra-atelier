const array = []

const potus = {
    nombre: "Potus",
    id: 1,
}

const prompt1 = parseInt(prompt("Pone el primero"));
const prompt2 = parseInt(prompt("Pone el segundo"));
const prompt3 = parseInt(prompt("Pone el tercero"));

prompt1 = array.push()